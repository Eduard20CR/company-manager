package manager.entities;

public class Employee extends Person {

    public Employee(String id, String name, String lastName, String username, String password, int partyTimes,
            int workTimes) {
        super(id, name, lastName, username, password, partyTimes, workTimes);
    }
}
